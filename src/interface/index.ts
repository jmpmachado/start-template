/**
 * @packageDocumentation
 * @module interface
 *
 * @remarks
 * Presentation boundary — HTTP/WS routes, CLI handlers, response mappers.
 * Translates external requests into application use-case calls; no business logic here.
 */
export const INTERFACE_LAYER = 'interface' as const;
